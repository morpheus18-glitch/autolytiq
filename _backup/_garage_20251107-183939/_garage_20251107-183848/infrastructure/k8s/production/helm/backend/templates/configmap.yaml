apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "autolytiq-backend.fullname" . }}-config
  labels:
    {{- include "autolytiq-backend.labels" . | nindent 4 }}
data:
  app-config.json: |
    {
      "logLevel": "info"
    }
