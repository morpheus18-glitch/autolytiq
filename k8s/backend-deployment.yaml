apiVersion: apps/v1
kind: Deployment
metadata:
  name: autolytiq-backend
  labels:
    app: autolytiq
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: autolytiq
      component: backend
  template:
    metadata:
      labels:
        app: autolytiq
        component: backend
    spec:
      # Run on general pool, not ML pool
      nodeSelector:
        doks.digitalocean.com/node-pool: pool-autolytiq
      containers:
      - name: backend
        image: autolytiq-backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: PORT
          value: "3000"
        - name: NODE_ENV
          value: "production"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: autolytiq-secrets
              key: jwt-secret
        - name: FRONTEND_URL
          value: "https://autolytiq.yourdomain.com"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 60
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: autolytiq-backend
  labels:
    app: autolytiq
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: autolytiq
    component: backend
