import { defineConfig } from "tsup";

export default defineConfig({
  entry: {
    index: "src/index.ts",
  },
  format: ["esm"],
  target: "node20",
  dts: true,
  sourcemap: false,
  clean: false, // Preserve tokens.css and tokens.ts generated by build script
  minify: false,
  splitting: false,
  bundle: true,
  onSuccess: 'echo "✅ Tokens package built successfully"',
});
