apiVersion: v1
kind: Service
metadata:
  name: {{ include "autolytiq-backend.fullname" . }}
  labels:
    {{- include "autolytiq-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autolytiq-backend.selectorLabels" . | nindent 4 }}
