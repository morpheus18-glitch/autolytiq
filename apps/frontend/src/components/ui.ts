/**
 * UI Component Library Barrel Export
 *
 * This file re-exports all components from @repo/ui for easier imports.
 * Usage: import { Button, Card, Input } from '@repo/ui';
 *
 * IMPORTANT: Always use these components instead of:
 * - Direct Radix UI imports
 * - Inline Tailwind classes
 * - Custom styled components
 */

// Re-export everything from @repo/ui
export * from '@repo/ui';

// For backwards compatibility, you can also import directly from @repo/ui
// import { Button } from '@repo/ui';
