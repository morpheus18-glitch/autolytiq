apiVersion: v1
kind: Service
metadata:
  name: backend-go
  namespace: autolytiq-prod
  labels:
    app: backend-go
    tier: backend
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: backend-go
